---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '7b48843e-5115-47d5-a2e7-0c28e0765716'
  PropagateID: '7b48843e-5115-47d5-a2e7-0c28e0765716'
  ReservedCode1: 'a9e780f6-df67-40c0-aa90-7b6f5a08ded5'
  ReservedCode2: 'a9e780f6-df67-40c0-aa90-7b6f5a08ded5'
---

# Java基础--IO

## 整体表现

本次共 5 题（文档题号 79~83），作为首次接触 IO 相关知识点的初学者，在概念理解上经历了从混淆到逐步厘清的过程。最突出的进步是第 80 题（同步/异步）经过多轮纠正，最终准确区分了"同步/异步"与"阻塞/非阻塞"两个维度。序列化/反序列化和阻塞/非阻塞掌握较好，Java 8 新特性覆盖不够完整。

## 各题表现概览

| 题号 | 题目 | 状态 | 评价 |
|---|---|---|---|
| 79 | BIO/NIO/AIO 区别 | 已作答（2轮） | 三种模式定性正确，第二轮补上 NIO selector 机制；缺 AIO 适用场景、JDK 版本锚点 |
| 80 | 同步和异步的区别 | 已作答（3轮） | 前两轮将同步/异步与阻塞/非阻塞混淆，第三轮纠正后准确答出"主动等待 vs 不等待+通知"；缺"状态、回调"两种获知方式 |
| 81 | 阻塞和非阻塞的区别 | 已作答（2轮） | 两轮均正确，方向准确；可升级表述为"线程被挂起/恢复运行" |
| 82 | 序列化和反序列化 | 已作答（2轮） | 第一轮只说了应用场景未给定义，第二轮准确定义"对象↔字节流"并补充场景，满分 |
| 83 | Java 8 新特性 | 已作答（先看答案再答） | 答出 Lambda、默认方法两点且理解到位；缺 Stream API、Optional、Date Time API |

## 需重点学习

### 1. 同步/异步 vs 阻塞/非阻塞的区分（反复混淆，最高优先级）

- **同步/异步**关注"结果由谁等、怎么获知"——关键词：主动等待、状态、通知、回调
- **阻塞/非阻塞**关注"等待时线程是否被挂起"——关键词：挂起、恢复运行
- 两者是独立维度，可交叉组合（如同步+非阻塞）
- 判定标准：同步≠一定阻塞，异步≠一定非阻塞

### 2. NIO 的 selector 多路复用机制

- 连接注册到多路复用器（selector）→ selector 轮询 → 有 IO 请求才启动线程处理
- 这是 NIO 与 BIO 的核心区别：BIO 每个连接占一个线程死等，NIO 只有真正有数据时才派线程

### 3. AIO 适用场景 + JDK 版本锚点

- AIO 适合连接多且长连接（重操作）的场景，如相册服务器
- JDK 版本：BIO 在 JDK1.4 前唯一，NIO 从 1.4 开始，AIO 从 1.7 开始

### 4. Java 8 新特性完整清单

除 Lambda 和默认方法外，还需掌握：
- **Stream API**：对集合做链式操作（过滤、映射、排序）
- **Optional 类**：用包装类代替 null，避免空指针异常
- **Date Time API**：新的日期时间类（LocalDateTime 等），替代老旧的 Date/Calendar

> AI生成